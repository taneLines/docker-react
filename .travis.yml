language: generic
sudo: required
services:
        - docker

before_install:
        - docker build -t rado/docker-react -f Dockerfile.dev .
script:
        - docker run -e CI=true rado/docker-react npm run test -- --coverage
